import React from 'react';
import { Layout } from 'antd';
import './App.css';

const {Content} = Layout;

const App: React.FC = () => {
    return (
        <Layout className="layout">
            <Content>

            </Content>
        </Layout>);
}; 

export default App;
